<.header>
  Listing Seeds
  <:subtitle>A seed is an installable configuration that can become a tree.</:subtitle>
</.header>

<.table
  id="seeds"
  rows={@streams.seeds}
  row_click={fn {_id, seed} -> JS.navigate(~p"/seeds/#{seed}") end}
>
  <:col :let={{_id, seed}} label="name"><%= seed.name %></:col>
  <:col :let={{_id, seed}} label="seed_type"><%= seed.seed_type %></:col>
  <:col :let={{_id, seed}} label="updated"><%= seed.updated_at %></:col>
  <:col :let={{_id, seed}} label="out_path"><%= seed.out_path %></:col>
  <:action :let={{_id, seed}}>
    <div class="sr-only">
      <.link navigate={~p"/seeds/#{seed}"}>Show</.link>
    </div>
  </:action>
</.table>
