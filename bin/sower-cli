#!/usr/bin/env bash
# Dev shim: lets `cargo run -- build` and the Rust `sower build` forwarder
# reach the Elixir CLI in the workspace via SOWER_BUILD_CLI.
set -euo pipefail
cd "$(dirname "$0")/.."
exec mix cli.run "$@"
